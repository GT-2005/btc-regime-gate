# Matches the stack described in the LATENT technical report.
# Nothing exotic — this runs on a fresh Colab instance or any local machine.

numpy>=1.24          # array math, matrix ops, random sampling
pandas>=2.0          # DataFrames, rolling windows, ewm, pct_change
scipy>=1.10          # stats.norm.logpdf (HMM emissions), special.logsumexp,
                     # optimize.minimize (L-BFGS-B, for GARCH MLE)
xgboost>=1.7         # XGBClassifier — 600 trees, depth 5, lr 0.03
lightgbm>=3.3        # LGBMClassifier — 600 estimators, 40 leaves, lr 0.03
scikit-learn>=1.3    # LogisticRegression meta-learner, StandardScaler, roc_auc_score

openpyxl>=3.1        # lets pandas read the Binance .xlsx export
requests>=2.31        # downloading candles from Binance
matplotlib>=3.8       # equity curve plot
reportlab>=4.0        # rebuilding the technical report (docs/make_report.py)
